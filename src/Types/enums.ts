export enum ButtonsTags {
   submit = "submit",
   div = "div",
}

export enum ButtonsBgColor {
   green = "#C3F598",
   pink = "#6933B0",
   white = "#F1F1F1",
}

export enum TitleVariants {
   h1 = "h1",
   h2 = "h2",
   h3 = "h3",
}

export enum ButtonsBgVariants {
   wide,
   narrow,
}

export enum HeaderType {
   auth,
   cabinet,
}
